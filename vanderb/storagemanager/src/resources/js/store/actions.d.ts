declare const _default: {
    getFiles({ state, commit }: any): void;
    changeDirectory({ state, dispatch, commit }: any, dir: any): void;
    refreshDirectory({ state, dispatch }: any): void;
};
export default _default;
